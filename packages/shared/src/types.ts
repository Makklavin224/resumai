// Moved into ./index.ts to keep Turbopack (Next.js) and NodeNext (Fastify)
// resolving the workspace package without re-export suffix ambiguity.
// This file is intentionally empty; import from '@resumai/shared' directly.
export {};
